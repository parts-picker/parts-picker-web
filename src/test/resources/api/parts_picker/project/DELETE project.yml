info:
  name: DELETE project
  type: http
  seq: 16

http:
  method: DELETE
  url: "{{base_url}}/projects/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
