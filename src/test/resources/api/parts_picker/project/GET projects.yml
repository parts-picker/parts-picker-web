info:
  name: GET projects
  type: http
  seq: 11

http:
  method: GET
  url: "{{base_url}}/projects"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
