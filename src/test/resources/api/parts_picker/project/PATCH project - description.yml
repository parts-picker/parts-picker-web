info:
  name: PATCH project - description
  type: http
  seq: 15

http:
  method: PATCH
  url: "{{base_url}}/projects/1"
  auth: inherit
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
      	"description": "Explore distant galaxies, solve cosmic mysteries, and journey through the cosmos in this immersive space exploration game. Uncover hidden secrets, trade with alien species, and become a renowned spacefarer in this epic adventure."
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
