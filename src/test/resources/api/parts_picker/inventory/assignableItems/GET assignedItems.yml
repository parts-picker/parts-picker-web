info:
  name: GET assignedItems
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/projects/2/required/2/assigned"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
