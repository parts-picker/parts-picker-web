info:
  name: GET assignableItems
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/projects/1/required/2/assignable"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
