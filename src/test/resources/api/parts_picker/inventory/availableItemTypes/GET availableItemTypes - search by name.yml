info:
  name: GET availableItemTypes - search by name
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/projects/1/available?name=small"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
