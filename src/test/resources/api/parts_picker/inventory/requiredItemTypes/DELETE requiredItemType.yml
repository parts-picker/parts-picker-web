info:
  name: DELETE requiredItemType
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{base_url}}/projects/1/required/2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
