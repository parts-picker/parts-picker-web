info:
  name: POST requiredItemType
  type: http
  seq: 5

http:
  method: POST
  url: "{{base_url}}/projects/1/required/2"
  auth: inherit
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
      	"requiredAmount": 10
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
