info:
  name: GET requiredItemTypes by project
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/projects/5/required"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
