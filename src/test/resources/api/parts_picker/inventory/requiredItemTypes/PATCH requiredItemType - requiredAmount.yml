info:
  name: PATCH requiredItemType - requiredAmount
  type: http
  seq: 7

http:
  method: PATCH
  url: "{{base_url}}/projects/1/required/1"
  auth: inherit
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
      	"requiredAmount": 10
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
