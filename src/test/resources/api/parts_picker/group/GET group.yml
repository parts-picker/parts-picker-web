info:
  name: GET group
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}/groups/2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
