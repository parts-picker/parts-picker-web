info:
  name: DELETE group
  type: http
  seq: 10

http:
  method: DELETE
  url: "{{base_url}}/groups/2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
