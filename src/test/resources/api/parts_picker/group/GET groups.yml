info:
  name: GET groups
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}/group"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
