info:
  name: GET instanceInfo by instanceId
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/instance/7/node"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
