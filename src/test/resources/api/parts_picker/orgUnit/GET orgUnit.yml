info:
  name: GET orgUnit
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/org-units/{{org_unit_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
