info:
  name: GET orgUnits
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/org-units"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
