info:
  name: POST orgUnit
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/org-units"
  auth: inherit
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
      	"name": "POSTED ORG UNIT",
      	"shortDescription": "POST WORKED"
      }

runtime:
  scripts:
    - type: after-response
      code: |-
        bru.setVar("org_unit_id", res.body._links.self.href.split("/").pop());

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
