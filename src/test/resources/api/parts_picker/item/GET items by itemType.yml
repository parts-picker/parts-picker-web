info:
  name: GET items by itemType
  type: http
  seq: 9

http:
  method: GET
  url: "{{base_url}}/item-types/1/items"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
