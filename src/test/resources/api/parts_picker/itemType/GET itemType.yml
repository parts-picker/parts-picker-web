info:
  name: GET itemType
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}/item-types/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
