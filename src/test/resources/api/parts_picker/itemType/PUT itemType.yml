info:
  name: PUT itemType
  type: http
  seq: 9

http:
  method: PUT
  url: "{{base_url}}/item-types/94"
  auth: inherit
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
      	"name": "An updated item type",
      	"description": "The updated description of an item type"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
