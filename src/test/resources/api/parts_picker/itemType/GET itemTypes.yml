info:
  name: GET itemTypes
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}/item-types?page=0"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
